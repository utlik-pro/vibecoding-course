# Модуль 10: Git, API и Payments для вайбкодеров

## Обзор модуля
- **Длительность:** ~4.5 часа (9 уроков)
- **Уровень:** Начинающий (zero code background)
- **Практический проект:** Data Aggregator (агрегатор данных с Premium подпиской)
- **Технологии:** Git, GitHub, Next.js API Routes, Stripe, OpenWeatherMap, ExchangeRate-API

## Цели обучения
После завершения модуля студенты смогут:
1. Понимать концепцию версионного контроля и использовать Git через Cursor
2. Создавать репозитории на GitHub и работать с ветками
3. Интегрировать внешние API (погода, курсы валют)
4. Обрабатывать ошибки и loading states при работе с API
5. Настраивать Stripe для приема платежей
6. Создавать Checkout Sessions и обрабатывать Webhooks
7. Реализовать базовую систему подписок

## Структура модуля

| Часть | Уроки | Тема |
|-------|-------|------|
| A | 10.1 - 10.4 | Git и GitHub |
| B | 10.5 - 10.6 | Внешние API |
| C | 10.7 - 10.9 | Stripe Payments |

---

# Часть A: Git и GitHub для вайбкодеров

---

## Урок 10.1: Что такое Git и зачем он нужен (20 мин)

### Цели урока
- Понять концепцию версионного контроля
- Узнать зачем нужен Git
- Увидеть реальные сценарии использования

### Содержание

#### 1. Аналогия "Сохранение в игре" (5 мин)

Представьте, что вы играете в сложную игру:
- Перед боссом вы **сохраняетесь**
- Если проиграли — **загружаете сохранение**
- У вас может быть **несколько слотов сохранений**

**Git работает точно так же, но для кода:**

```
Git термин     = Игровая аналогия
─────────────────────────────────
Commit         = Одно сохранение
Branch         = Слот сохранения / параллельная вселенная
Checkout       = Загрузить сохранение
Push           = Отправить сохранения в облако
Pull           = Скачать сохранения из облака
Merge          = Объединить два прохождения
Repository     = Папка со всеми сохранениями
GitHub         = Облачное хранилище сохранений
```

#### 2. Зачем Git вайбкодеру? (5 мин)

**Проблема без Git:**
```
Ситуация: Попросили AI "улучшить код"
AI: *переписал половину проекта*
Вы: "Эээ... верни как было!"
AI: "Не помню как было :("
Вы: *плачет*
```

**С Git:**
```
Вы: git checkout main
Код: *вернулся к последнему сохранению*
Вы: *счастлив*
```

**Причины использовать Git:**

| Причина | Объяснение |
|---------|------------|
| Безопасность | AI иногда ломает код — можно откатиться |
| Эксперименты | Пробуем новые идеи без страха |
| Бекап | Код хранится в облаке (GitHub) |
| Портфолио | GitHub профиль = резюме разработчика |
| Деплой | Vercel/Netlify работают с GitHub |

#### 3. Git vs GitHub (5 мин)

```
Git    = Программа на вашем компьютере
GitHub = Сайт для хранения кода в облаке

Аналогия:
Git    = Microsoft Word (редактор на компьютере)
GitHub = OneDrive (облачное хранилище)
```

**Другие "облака" для Git:**
- GitHub (самый популярный)
- GitLab
- Bitbucket

Мы используем GitHub — он бесплатный и там все разработчики.

#### 4. Что такое Push и Pull (5 мин)

**Push и Pull — это "мост" между вашим компьютером и GitHub:**

```
Push = Отправить свой код на GitHub (из компьютера → в облако)
Pull = Скачать код с GitHub (из облака → на компьютер)
```

**Аналогия с Google Drive:**
```
Вы написали документ в Word → Сохранили на Google Drive
                                (это Push)

Открыли тот же документ на другом компьютере из Google Drive
                                (это Pull)
```

**Зачем нужен Push:**
- Ваш код попадает в облако (бекап!)
- Другие люди могут видеть ваш код
- Vercel/Netlify автоматически деплоят при Push
- GitHub показывает историю изменений

**Зачем нужен Pull:**
- Скачать изменения, которые сделали другие
- Синхронизировать код между компьютерами
- Получить обновления проекта

**Полный цикл работы с Git:**
```
1. Написал код
2. Commit  → Сохранил локально (на своём компьютере)
3. Push    → Отправил в облако (на GitHub)

Другой компьютер:
4. Pull    → Скачал из облака
5. Написал код
6. Commit + Push → Отправил обратно
```

**Важно:** Commit сохраняет только ЛОКАЛЬНО. Чтобы код попал на GitHub, нужен Push!

```
Commit без Push = Сохранили файл, но не загрузили в облако
                  (если компьютер сломается — код пропадёт!)

Commit + Push   = Сохранили файл И загрузили в облако
                  (код в безопасности!)
```

#### 5. Демонстрация: проблема без Git (5 мин)

**Сценарий 1: AI сломал код**
```
До:  Рабочее приложение
После промпта AI: Белый экран, 50 ошибок

Без Git: "Всё пропало! Придётся начинать заново..."
С Git:   git checkout . → Всё работает за 1 секунду
```

**Сценарий 2: Хочу попробовать новую идею**
```
Без Git: "А вдруг сломаю? Лучше не буду..."
С Git:   Создаю ветку → Экспериментирую →
         Получилось? Merge!
         Не получилось? Удаляю ветку, main не пострадал
```

### Quiz

**Вопрос 1:** Что такое commit в Git?
- [ ] Папка с файлами
- [x] Сохранение текущего состояния кода
- [ ] Облачный сервер
- [ ] Команда для удаления кода

**Вопрос 2:** Зачем нужен GitHub?
- [ ] Только для хранения кода
- [ ] Только для командной работы
- [x] Хранение, бекап, командная работа, деплой
- [ ] Для запуска кода

**Вопрос 3:** Что такое Push?
- [ ] Сохранение файла на компьютере
- [ ] Скачивание кода из интернета
- [x] Отправка commits на GitHub (из компьютера в облако)
- [ ] Удаление файлов

**Вопрос 4:** Что делает ветка (branch) в Git?
- [ ] Удаляет файлы
- [x] Создаёт параллельную версию кода для экспериментов
- [ ] Отправляет код в облако
- [ ] Скачивает код из интернета

**Вопрос 5:** Чем Commit отличается от Push?
- [ ] Ничем, это одно и то же
- [x] Commit сохраняет локально, Push отправляет на GitHub
- [ ] Push сохраняет локально, Commit отправляет на GitHub
- [ ] Оба отправляют код на GitHub

### Домашнее задание
Зарегистрируйтесь на github.com и придумайте профессиональный username.

---

## Урок 10.2: Git через Cursor - базовые операции (30 мин)

### Цели урока
- Научиться использовать Git без командной строки
- Освоить init, add, commit через интерфейс Cursor
- Сделать первые commits

### Содержание

#### 1. Открываем Git в Cursor (5 мин)

**Способ 1: Боковая панель**
- Нажимаем на иконку с веточками слева (Source Control)
- Или горячая клавиша: `Ctrl+Shift+G` (Windows) / `Cmd+Shift+G` (Mac)

**Что видим:**
```
SOURCE CONTROL
─────────────────
📁 Changes (3)      ← Изменённые файлы
  M  app/page.tsx   ← M = Modified (изменён)
  U  lib/utils.ts   ← U = Untracked (новый файл)
  D  old-file.ts    ← D = Deleted (удалён)
```

#### 2. Инициализация репозитория (5 мин)

Если папка ещё не Git репозиторий:

**Способ 1: Через интерфейс**
- Source Control → "Initialize Repository"

**Способ 2: Через AI (рекомендуется)**

```
Промпт для Cursor:
"Инициализируй Git репозиторий в текущей папке
и создай .gitignore для Next.js проекта"
```

**Результат:**
- Появится скрытая папка `.git`
- Создастся файл `.gitignore`

**Содержимое .gitignore для Next.js:**
```gitignore
# dependencies
node_modules/

# next.js
.next/
out/

# environment
.env
.env.local
.env*.local

# misc
.DS_Store
*.log
```

#### 3. Первый commit (10 мин)

**Шаг 1: Смотрим изменения**
- В Source Control видим список файлов
- Кликаем на файл — видим что изменилось (diff)

**Шаг 2: Добавляем файлы (Stage)**
- Наводим на файл → нажимаем `+`
- Или наводим на "Changes" → нажимаем `+` (добавить все)

**Шаг 3: Пишем сообщение**
- В поле сверху пишем что сделали
- Например: "Добавил главную страницу"

**Шаг 4: Commit**
- Нажимаем галочку ✓ (или Ctrl+Enter)

**Правила хороших сообщений:**
```
✅ Хорошо:
- "Добавил форму регистрации"
- "Исправил баг с отображением цены"
- "Обновил стили кнопок"
- "Удалил неиспользуемые компоненты"

❌ Плохо:
- "fix"
- "update"
- "asdfasdf"
- "123"
- "работает"
```

**Формат сообщения:**
```
Что сделал + где/что затронуто

Примеры:
"Добавил кнопку выхода на Dashboard"
"Исправил отображение даты в карточке товара"
"Обновил цвета в тёмной теме"
```

#### 4. Практика: делаем 3 commits (10 мин)

**Задание:** Создайте 3 commits в своём проекте

**Commit 1: README**
```markdown
# Создайте файл README.md:

# Data Aggregator

Приложение для агрегации данных из внешних API.

## Технологии
- Next.js 14
- TypeScript
- Tailwind CSS
```
Сообщение: `"Добавил README с описанием проекта"`

**Commit 2: Обновление README**
```markdown
# Добавьте секцию в README.md:

## Установка

1. Клонировать репозиторий
2. Установить зависимости: `npm install`
3. Запустить: `npm run dev`
```
Сообщение: `"Добавил инструкции по установке в README"`

**Commit 3: Gitignore**
- Если ещё нет `.gitignore` — создайте
- Добавьте правила для Next.js

Сообщение: `"Добавил .gitignore для Next.js"`

### Промпт для AI помощи

```
Помоги мне сделать commit в Git:
1. Покажи какие файлы изменились
2. Объясни что делает каждое изменение
3. Предложи хорошее сообщение для commit
```

### Checklist урока
- [ ] Source Control открывается
- [ ] Git инициализирован в проекте
- [ ] .gitignore создан
- [ ] Сделано минимум 3 commits
- [ ] История коммитов видна в Source Control

---

## Урок 10.3: GitHub - облачное хранилище кода (25 мин)

### Цели урока
- Создать аккаунт на GitHub
- Создать репозиторий
- Связать локальный проект с GitHub
- Сделать push

### Содержание

#### 1. Регистрация на GitHub (5 мин)

**Шаги:**
1. Переходим на [github.com](https://github.com)
2. Нажимаем "Sign up"
3. Вводим:
   - Email (рабочий, который проверяете)
   - Пароль (надёжный!)
   - Username (важно!)

**Как выбрать username:**
```
✅ Хорошо (профессионально):
- ivan-petrov
- maria-dev
- alex-frontend
- yourname-code

❌ Плохо (не берут на работу):
- xXx_killer_xXx
- qwerty123
- test12345
- coolhacker2005
```

**Почему важен username:**
- Это ваше профессиональное имя
- Работодатели смотрят GitHub
- Ссылка будет: github.com/ваш-username

4. Подтверждаем email
5. Выбираем Free план (бесплатный, всего хватит)

#### 2. Создание репозитория (5 мин)

**На GitHub:**
1. Нажимаем `+` в правом верхнем углу
2. "New repository"
3. Заполняем:
   - **Repository name:** `data-aggregator`
   - **Description:** `Агрегатор данных - курс Vibecoding`
   - **Public** (чтобы показать работодателям)
   - **НЕ ставим галочки** (Add README, .gitignore) — у нас уже есть!
4. "Create repository"

**Что видим:**
- GitHub показывает инструкции для подключения
- Нам нужен URL: `https://github.com/username/data-aggregator.git`

#### 3. Подключаем GitHub к Cursor (10 мин)

**Способ 1: Через интерфейс Cursor**

1. Source Control → три точки `...` → "Remote" → "Add Remote"
2. Вводим URL репозитория
3. Cursor попросит авторизоваться в GitHub
4. Разрешаем доступ

**Способ 2: Через AI (проще)**

```
Промпт для Cursor:
"Подключи мой локальный Git репозиторий к GitHub.
URL репозитория: https://github.com/username/data-aggregator.git
Покажи шаги и выполни команды."
```

**Что произойдёт:**
```bash
# AI выполнит:
git remote add origin https://github.com/username/data-aggregator.git
git branch -M main
git push -u origin main
```

**Авторизация GitHub:**
- Cursor откроет браузер
- Войдите в GitHub
- Разрешите доступ для Cursor/VS Code
- Вернитесь в редактор

#### 4. Первый Push (5 мин)

**Push = отправить commits на GitHub**

**Способ 1: Через интерфейс**
- Source Control → три точки `...` → "Push"
- Или иконка облака со стрелкой вверх

**Способ 2: Горячие клавиши**
- `Cmd+Shift+P` (Mac) / `Ctrl+Shift+P` (Windows)
- Набираем "Git: Push"
- Enter

**Проверяем на GitHub:**
1. Открываем github.com/username/data-aggregator
2. Обновляем страницу
3. Видим все наши файлы!

#### 5. Практика: цикл изменений (5 мин)

**Полный цикл работы:**
```
1. Меняем код
2. Source Control: видим изменения
3. Stage (нажимаем +)
4. Пишем сообщение
5. Commit (галочка ✓)
6. Push (отправляем в облако)
7. Проверяем на GitHub
```

**Задание:**
1. Добавьте что-нибудь в README.md
2. Сделайте commit с сообщением
3. Push
4. Проверьте на GitHub — изменения появились?

### Checklist урока
- [ ] Аккаунт GitHub создан
- [ ] Username профессиональный
- [ ] Репозиторий создан на GitHub
- [ ] Локальный проект связан с GitHub (remote добавлен)
- [ ] Первый push выполнен
- [ ] Код виден на GitHub

---

## Урок 10.4: Ветки и работа с изменениями (25 мин)

### Цели урока
- Понять концепцию веток
- Научиться создавать и переключать ветки
- Освоить merge через Cursor

### Содержание

#### 1. Что такое ветки? (5 мин)

**Аналогия: Параллельные вселенные**

```
                    Вселенная 1 (main)
                    Стабильная версия
                    ─────────────────────────────────────>
                         \                           /
                          \    Вселенная 2         /
                           \   (эксперимент)      /
                            └────────────────────┘
                              Если удалось → merge
                              Если нет → удаляем
```

**Зачем это вайбкодеру:**
```
1. Хочу попробовать новый дизайн
   → Создаю ветку feature/new-design
   → Экспериментирую с AI
   → Понравилось? Merge в main
   → Не понравилось? Удаляю ветку, main чистый

2. AI предлагает "улучшить архитектуру"
   → Создаю ветку experiment/ai-refactor
   → Даю AI переписать код
   → Работает? Отлично, merge!
   → Сломалось? Удаляю, ничего не потеряно
```

#### 2. Создание ветки (5 мин)

**Способ 1: Через интерфейс Cursor**
1. Внизу слева видим название ветки (обычно `main`)
2. Кликаем на него
3. "Create new branch..."
4. Вводим имя: `feature/add-weather`

**Способ 2: Через AI**
```
Промпт для Cursor:
"Создай новую Git ветку с названием feature/add-weather-widget
и переключись на неё"
```

**Правила именования веток:**
```
Формат: тип/описание

✅ Хорошие названия:
- feature/add-login        (новая функция)
- feature/weather-widget   (новая функция)
- fix/payment-bug          (исправление бага)
- fix/mobile-layout        (исправление)
- update/readme            (обновление)
- experiment/new-design    (эксперимент)

❌ Плохие названия:
- new-stuff
- test
- aaa
- branch1
```

#### 3. Работа в ветке (5 мин)

**После создания ветки:**
1. Внизу слева видим новое имя ветки
2. Все изменения теперь в этой ветке
3. `main` остаётся нетронутым

**Делаем изменения:**
```
1. Пишем код / даём задание AI
2. Stage изменения (+)
3. Commit с сообщением
4. Push

При первом push Cursor спросит:
"Branch 'feature/add-weather' has no upstream branch.
Would you like to publish this branch?"
→ Нажимаем "OK" или "Publish Branch"
```

#### 4. Переключение между ветками (5 мин)

**Способ 1: Через интерфейс**
1. Кликаем на имя ветки внизу слева
2. Выбираем нужную ветку из списка

**Способ 2: Через AI**
```
Промпт: "Переключись на ветку main"
```

**Важно:**
- При переключении код меняется!
- Изменения из одной ветки не видны в другой
- Это нормально и правильно

#### 5. Merge — объединение веток (5 мин)

**Когда всё готово и работает:**

**Способ 1: Через интерфейс Cursor**
1. Переключаемся на `main`
2. Source Control → три точки `...` → "Branch" → "Merge Branch..."
3. Выбираем нашу ветку (feature/add-weather)
4. Push

**Способ 2: Через AI**
```
Промпт для Cursor:
"Слей ветку feature/add-weather в main:
1. Переключись на main
2. Сделай merge feature/add-weather
3. Push изменения"
```

**После merge:**
- Изменения из ветки теперь в main
- Ветку можно удалить (она больше не нужна)

### Практика: создаём и сливаем ветку

**Задание:**
```
1. Создайте ветку: feature/test-branch
2. Создайте файл: test.txt с текстом "Это тест веток"
3. Commit: "Добавил тестовый файл"
4. Переключитесь на main
   → Файл test.txt пропал (это нормально!)
5. Merge feature/test-branch в main
   → Файл test.txt появился
6. Push
7. Удалите ветку feature/test-branch (опционально)
```

### Промпт для всего процесса
```
Помоги мне поработать с ветками Git:
1. Создай ветку feature/test-experiment
2. В этой ветке создай файл experiment.md
3. Сделай commit
4. Переключись на main
5. Покажи что файла нет в main
6. Слей ветку в main
7. Покажи что файл появился
```

### Checklist урока
- [ ] Понимаю зачем нужны ветки
- [ ] Умею создавать ветки через Cursor
- [ ] Умею переключаться между ветками
- [ ] Сделал merge ветки в main
- [ ] Push работает

---

# Часть B: Интеграция внешних API

---

## Урок 10.5: Что такое внешние API (20 мин)

### Цели урока
- Понять что такое внешние API
- Увидеть примеры популярных API
- Понять как это работает

### Содержание

#### 1. Аналогия "Доставка еды" (5 мин)

```
Вы хотите показать погоду в приложении.
Но у вас нет метеостанций!

Решение: "заказать" данные у тех, у кого они есть.

Ваше приложение  →  OpenWeatherMap API  →  Данные о погоде
                    (запрос)                (ответ)
```

**Как доставка еды:**
| Доставка еды | API |
|--------------|-----|
| Вы звоните в ресторан | HTTP запрос |
| Говорите что хотите | Параметры запроса |
| Вам привозят еду | JSON ответ |
| Платите за заказ | API ключ / лимиты |

#### 2. Популярные бесплатные API (5 мин)

| API | Что даёт | Бесплатный лимит | Сайт |
|-----|----------|------------------|------|
| OpenWeatherMap | Погода | 1000 запросов/день | openweathermap.org |
| ExchangeRate-API | Курсы валют | 1500/месяц | exchangerate-api.com |
| NewsAPI | Новости | 100/день | newsapi.org |
| JSONPlaceholder | Тестовые данные | Безлимитно | jsonplaceholder.typicode.com |
| CatAPI | Фото котиков | Безлимитно | thecatapi.com |
| RandomUser | Случайные пользователи | Безлимитно | randomuser.me |

**Для нашего проекта используем:**
- OpenWeatherMap (погода)
- ExchangeRate-API (курсы валют)

#### 3. Как выглядит запрос и ответ (5 мин)

**Запрос (Request):**
```
GET https://api.openweathermap.org/data/2.5/weather?q=Moscow&appid=YOUR_KEY&units=metric&lang=ru
    ↑                                              ↑         ↑            ↑           ↑
    метод                                          город    API ключ    единицы    язык
```

**Ответ (Response) — JSON:**
```json
{
  "name": "Moscow",
  "main": {
    "temp": 5.2,
    "humidity": 80,
    "pressure": 1015
  },
  "weather": [
    {
      "main": "Clouds",
      "description": "облачно"
    }
  ],
  "wind": {
    "speed": 3.5
  }
}
```

**Как читать JSON:**
```javascript
const data = await response.json()

data.name           // "Moscow"
data.main.temp      // 5.2
data.weather[0].description  // "облачно"
```

#### 4. API ключи — зачем они нужны (5 мин)

**API ключ = пропуск в "ресторан"**
- Идентифицирует вас
- Считает ваши запросы
- Ограничивает бесплатный лимит

**Пример:**
```
Без ключа: "Вы кто такой? Не обслуживаем!"
С ключом: "А, это вы! Пожалуйста, ваши данные."
```

**ВАЖНО — безопасность ключей:**
```
❌ НИКОГДА так:
const API_KEY = "abc123secretkey"  // В коде — ОПАСНО!

✅ ВСЕГДА так:
// .env.local (этот файл НЕ попадает на GitHub)
WEATHER_API_KEY=abc123secretkey

// В коде:
const API_KEY = process.env.WEATHER_API_KEY
```

**Почему это важно:**
- Код на GitHub виден всем
- Кто-то может использовать ваш ключ
- Исчерпает ваш лимит
- Или получит доступ к платным функциям

### Quiz

**Вопрос 1:** Что такое API?
- [ ] Язык программирования
- [x] Способ получить данные от внешнего сервиса
- [ ] База данных
- [ ] Тип файла

**Вопрос 2:** Что такое API ключ?
- [ ] Пароль от сервера
- [x] Идентификатор для доступа к API
- [ ] Адрес сервера
- [ ] Тип запроса

**Вопрос 3:** Где хранить API ключи?
- [ ] В коде напрямую
- [ ] В README.md
- [x] В файле .env.local
- [ ] В комментариях

---

## Урок 10.6: Интеграция внешних API в Next.js (35 мин)

### Цели урока
- Получить API ключи для погоды и курсов валют
- Создать API routes для получения данных
- Обработать ошибки и loading states

### Содержание

#### 1. Получение API ключей (10 мин)

**OpenWeatherMap (погода):**
1. Регистрация на [openweathermap.org](https://openweathermap.org)
2. Подтверждаем email
3. Переходим в "API Keys"
4. Видим ключ или нажимаем "Generate"
5. Копируем ключ (выглядит как: `a1b2c3d4e5f6...`)

**Важно:** Ключ активируется через 10-30 минут после создания!

**ExchangeRate-API (курсы валют):**
1. Регистрация на [exchangerate-api.com](https://exchangerate-api.com)
2. Подтверждаем email
3. Dashboard → видим API Key
4. Копируем ключ

**Добавляем в проект:**

Создайте файл `.env.local` в корне проекта:
```env
# API ключи (НЕ публикуйте их!)
OPENWEATHER_API_KEY=ваш_ключ_погоды
EXCHANGE_API_KEY=ваш_ключ_валют
```

**Проверьте .gitignore:**
```gitignore
# Должна быть строка:
.env.local
```

#### 2. API Route для погоды (10 мин)

**Промпт для Cursor:**
```
Создай API route /api/weather в Next.js App Router:

1. Файл: app/api/weather/route.ts
2. Принимает query параметр city (город)
3. Делает запрос к OpenWeatherMap API:
   - URL: https://api.openweathermap.org/data/2.5/weather
   - Параметры: q={city}, appid={key}, units=metric, lang=ru
4. API ключ берёт из process.env.OPENWEATHER_API_KEY
5. Возвращает JSON:
   {
     success: true,
     data: {
       city: "Москва",
       temp: 5,
       description: "облачно",
       humidity: 80,
       wind: 3.5
     }
   }
6. Обрабатывает ошибки:
   - Город не найден → 404
   - API недоступен → 500
   - Нет параметра city → 400

Используй TypeScript и обработку ошибок.
```

**Ожидаемый результат (app/api/weather/route.ts):**
```typescript
import { NextRequest, NextResponse } from 'next/server'

export async function GET(request: NextRequest) {
  const searchParams = request.nextUrl.searchParams
  const city = searchParams.get('city')

  if (!city) {
    return NextResponse.json(
      { success: false, error: 'Параметр city обязателен' },
      { status: 400 }
    )
  }

  const apiKey = process.env.OPENWEATHER_API_KEY

  try {
    const response = await fetch(
      `https://api.openweathermap.org/data/2.5/weather?q=${city}&appid=${apiKey}&units=metric&lang=ru`
    )

    if (!response.ok) {
      if (response.status === 404) {
        return NextResponse.json(
          { success: false, error: 'Город не найден' },
          { status: 404 }
        )
      }
      throw new Error('API error')
    }

    const data = await response.json()

    return NextResponse.json({
      success: true,
      data: {
        city: data.name,
        temp: Math.round(data.main.temp),
        description: data.weather[0].description,
        humidity: data.main.humidity,
        wind: data.wind.speed
      }
    })
  } catch (error) {
    return NextResponse.json(
      { success: false, error: 'Ошибка сервера' },
      { status: 500 }
    )
  }
}
```

**Тестируем:**
```
http://localhost:3000/api/weather?city=Moscow
http://localhost:3000/api/weather?city=Минск
http://localhost:3000/api/weather?city=НесуществующийГород
```

#### 3. API Route для курсов валют (10 мин)

**Промпт для Cursor:**
```
Создай API route /api/exchange в Next.js App Router:

1. Файл: app/api/exchange/route.ts
2. Принимает query параметр base (базовая валюта, по умолчанию USD)
3. Делает запрос к ExchangeRate-API:
   - URL: https://v6.exchangerate-api.com/v6/{key}/latest/{base}
4. API ключ из process.env.EXCHANGE_API_KEY
5. Возвращает курсы к: RUB, EUR, BYN, UAH
6. Формат ответа:
   {
     success: true,
     data: {
       base: "USD",
       rates: {
         RUB: 92.5,
         EUR: 0.92,
         BYN: 3.2,
         UAH: 41.5
       },
       updated: "2024-01-15T12:00:00"
     }
   }
7. Добавь кеширование на 1 час (revalidate: 3600)
```

**Тестируем:**
```
http://localhost:3000/api/exchange
http://localhost:3000/api/exchange?base=EUR
```

#### 4. Компоненты для отображения (5 мин)

**Промпт для виджета погоды:**
```
Создай React компонент WeatherWidget:

1. Файл: components/WeatherWidget.tsx
2. Props: city (string)
3. Функционал:
   - При монтировании делает запрос к /api/weather?city={city}
   - Показывает loading spinner во время загрузки
   - Показывает данные: город, температура, описание, влажность
   - Показывает ошибку если город не найден
4. Стили: Tailwind CSS, красивая карточка
5. Иконка погоды (можно эмодзи: ☀️ ⛅ 🌧️ ❄️)
```

**Промпт для виджета валют:**
```
Создай React компонент ExchangeWidget:

1. Файл: components/ExchangeWidget.tsx
2. Функционал:
   - Загружает курсы валют при монтировании
   - Показывает курс USD к RUB, EUR, BYN, UAH
   - Loading state и обработка ошибок
3. Стили: Tailwind CSS, таблица или список
4. Показывает время обновления
```

### Checklist урока
- [ ] API ключи получены и добавлены в .env.local
- [ ] .env.local в .gitignore
- [ ] /api/weather работает
- [ ] /api/exchange работает
- [ ] Ошибки обрабатываются
- [ ] Компоненты отображают данные

---

# Часть C: Stripe Payments

---

## Урок 10.7: Stripe — настройка и основы (25 мин)

### Цели урока
- Понять что такое Stripe
- Создать аккаунт и получить ключи
- Понять режим тестирования

### Содержание

#### 1. Что такое Stripe (5 мин)

**Stripe = платёжная система для разработчиков**

```
Что умеет Stripe:
✓ Принимать карты со всего мира
✓ Автоматически конвертировать валюту
✓ Обрабатывать подписки
✓ Безопасно хранить данные карт
✓ Возвращать деньги
✓ Отправлять чеки
```

**Почему Stripe:**
- Самая популярная в мире
- Отличная документация
- Готовые компоненты для React/Next.js
- Работает с Vercel из коробки

**Альтернативы:**
| Система | Регион | Особенности |
|---------|--------|-------------|
| Stripe | Международная | Стандарт индустрии |
| LemonSqueezy | Международная | Проще для цифровых товаров |
| Paddle | Международная | Берёт на себя налоги |
| YooKassa | Россия | Для РФ резидентов |

#### 2. Регистрация и Test Mode (10 мин)

**Шаги регистрации:**
1. Переходим на [stripe.com](https://stripe.com)
2. "Start now" или "Create account"
3. Вводим email и пароль
4. Подтверждаем email
5. **ВАЖНО: Остаёмся в Test mode!**

**Test Mode vs Live Mode:**

```
┌─────────────────────────────────────────────────┐
│              TEST MODE (тестовый)               │
├─────────────────────────────────────────────────┤
│ ✓ Карты НЕ списывают реальные деньги           │
│ ✓ Можно "платить" сколько угодно               │
│ ✓ Тестовая карта: 4242 4242 4242 4242          │
│ ✓ Любая дата в будущем, любой CVV              │
│ ✓ Идеально для разработки и обучения           │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│              LIVE MODE (боевой)                 │
├─────────────────────────────────────────────────┤
│ ⚠ Настоящие деньги!                            │
│ ⚠ Нужна верификация бизнеса                    │
│ ⚠ НЕ включаем пока не готовы к продакшену!    │
└─────────────────────────────────────────────────┘
```

**Как проверить режим:**
- В Dashboard вверху есть переключатель
- Test mode = оранжевая полоска
- Ключи начинаются с `pk_test_` и `sk_test_`

#### 3. Получение API ключей (5 мин)

**В Stripe Dashboard:**
1. Developers → API keys
2. Видим два ключа:

```
Publishable key: pk_test_51ABC...
(можно показывать в браузере)

Secret key: sk_test_51XYZ...
(ТОЛЬКО на сервере, никому не показывать!)
```

**Добавляем в .env.local:**
```env
# Stripe (Test Mode)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_ваш_ключ
STRIPE_SECRET_KEY=sk_test_ваш_ключ
```

**Важно про ключи:**
```
NEXT_PUBLIC_ = виден в браузере (только для publishable)
Без префикса = только на сервере (для secret)
```

#### 4. Установка Stripe (5 мин)

**Устанавливаем пакеты:**
```bash
npm install stripe @stripe/stripe-js
```

**Промпт для настройки:**
```
Настрой Stripe в Next.js проекте:

1. Создай lib/stripe.ts — серверный клиент Stripe
   - Использует STRIPE_SECRET_KEY
   - Экспортирует инстанс stripe

2. Создай lib/stripe-client.ts — клиентский клиент
   - Использует NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY
   - Использует loadStripe из @stripe/stripe-js
   - Экспортирует getStripe() функцию

Используй TypeScript.
```

**Ожидаемые файлы:**

```typescript
// lib/stripe.ts (серверный)
import Stripe from 'stripe'

export const stripe = new Stripe(process.env.STRIPE_SECRET_KEY!, {
  apiVersion: '2023-10-16',
  typescript: true,
})
```

```typescript
// lib/stripe-client.ts (клиентский)
import { loadStripe, Stripe } from '@stripe/stripe-js'

let stripePromise: Promise<Stripe | null>

export const getStripe = () => {
  if (!stripePromise) {
    stripePromise = loadStripe(
      process.env.NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY!
    )
  }
  return stripePromise
}
```

### Checklist урока
- [ ] Аккаунт Stripe создан
- [ ] Находимся в Test Mode
- [ ] API ключи добавлены в .env.local
- [ ] Пакеты stripe установлены
- [ ] lib/stripe.ts создан
- [ ] lib/stripe-client.ts создан

---

## Урок 10.8: Checkout Session и приём платежей (30 мин)

### Цели урока
- Создать продукт в Stripe
- Реализовать Checkout Session
- Протестировать платёж

### Содержание

#### 1. Как работает Stripe Checkout (5 мин)

```
Пользователь              Ваш сервер              Stripe
     │                         │                     │
     │── Нажал "Купить" ──────>│                     │
     │                         │── Создай сессию ───>│
     │                         │<── URL сессии ──────│
     │<── Редирект на URL ─────│                     │
     │                         │                     │
     │──────── Вводит карту ───────────────────────>│
     │                         │                     │
     │<──────── Успех! Редирект на /success ────────│
     │                         │                     │
     │                         │<── Webhook: оплачено│
```

**Преимущества Checkout:**
- Stripe сама показывает форму оплаты
- Мы не храним данные карт (безопасно!)
- Работает Apple Pay, Google Pay
- Адаптивный дизайн

#### 2. Создание продукта в Stripe (5 мин)

**В Stripe Dashboard:**
1. Products → Add product
2. Заполняем:
   - **Name:** "Premium подписка"
   - **Description:** "Доступ ко всем функциям Data Aggregator"
3. Pricing:
   - **Price:** $9.99
   - **Billing period:** Monthly (ежемесячно)
4. Save product

**Копируем Price ID:**
- Открываем созданный продукт
- В разделе Pricing видим Price ID: `price_1ABC...`
- Копируем его

**Добавляем в .env.local:**
```env
STRIPE_PRICE_ID=price_ваш_id
```

#### 3. API Route для создания сессии (10 мин)

**Промпт для Cursor:**
```
Создай API route /api/stripe/create-checkout-session:

1. Файл: app/api/stripe/create-checkout-session/route.ts
2. Метод: POST
3. Принимает в body: { priceId: string }
4. Создаёт Stripe Checkout Session:
   - mode: 'subscription' (подписка)
   - line_items: [{ price: priceId, quantity: 1 }]
   - success_url: {NEXT_PUBLIC_APP_URL}/success?session_id={CHECKOUT_SESSION_ID}
   - cancel_url: {NEXT_PUBLIC_APP_URL}/pricing
5. Возвращает: { url: session.url }
6. Обрабатывает ошибки
```

**Добавь в .env.local:**
```env
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

**Ожидаемый код:**
```typescript
// app/api/stripe/create-checkout-session/route.ts
import { NextResponse } from 'next/server'
import { stripe } from '@/lib/stripe'

export async function POST(request: Request) {
  try {
    const { priceId } = await request.json()

    const session = await stripe.checkout.sessions.create({
      mode: 'subscription',
      payment_method_types: ['card'],
      line_items: [
        {
          price: priceId,
          quantity: 1,
        },
      ],
      success_url: `${process.env.NEXT_PUBLIC_APP_URL}/success?session_id={CHECKOUT_SESSION_ID}`,
      cancel_url: `${process.env.NEXT_PUBLIC_APP_URL}/pricing`,
    })

    return NextResponse.json({ url: session.url })
  } catch (error) {
    console.error('Stripe error:', error)
    return NextResponse.json(
      { error: 'Ошибка создания сессии' },
      { status: 500 }
    )
  }
}
```

#### 4. Кнопка покупки (5 мин)

**Промпт для Cursor:**
```
Создай компонент PurchaseButton:

1. Файл: components/PurchaseButton.tsx
2. При клике:
   - Показывает loading
   - Вызывает POST /api/stripe/create-checkout-session
   - Передаёт priceId из env
   - Редиректит на session.url
3. Обрабатывает ошибки
4. Стили: красивая кнопка с Tailwind
5. Текст: "Оформить Premium — $9.99/мес"
```

**Ожидаемый компонент:**
```tsx
'use client'

import { useState } from 'react'

export default function PurchaseButton() {
  const [loading, setLoading] = useState(false)

  const handlePurchase = async () => {
    setLoading(true)
    try {
      const response = await fetch('/api/stripe/create-checkout-session', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          priceId: process.env.NEXT_PUBLIC_STRIPE_PRICE_ID
        }),
      })

      const { url } = await response.json()
      window.location.href = url
    } catch (error) {
      console.error('Error:', error)
      alert('Ошибка при создании сессии оплаты')
    } finally {
      setLoading(false)
    }
  }

  return (
    <button
      onClick={handlePurchase}
      disabled={loading}
      className="bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 px-6 rounded-lg disabled:opacity-50"
    >
      {loading ? 'Загрузка...' : 'Оформить Premium — $9.99/мес'}
    </button>
  )
}
```

**Добавьте в .env.local:**
```env
NEXT_PUBLIC_STRIPE_PRICE_ID=price_ваш_id
```

#### 5. Страницы success и cancel (5 мин)

**Промпт:**
```
Создай страницы для Stripe:

1. app/success/page.tsx
   - Показывает "Спасибо за покупку!"
   - Получает session_id из URL
   - Кнопка "Перейти в личный кабинет"

2. app/pricing/page.tsx (или где cancel_url)
   - Показывает тарифы
   - Кнопка PurchaseButton
```

### Тестирование платежа

**Шаги:**
1. Запускаем `npm run dev`
2. Открываем страницу с кнопкой покупки
3. Нажимаем "Оформить Premium"
4. Попадаем на Stripe Checkout

**Тестовые данные карты:**
```
Номер карты: 4242 4242 4242 4242
Срок: любая дата в будущем (например, 12/30)
CVV: любые 3 цифры (например, 123)
Имя: любое
```

5. Нажимаем "Оплатить"
6. Попадаем на /success

**Проверяем в Stripe Dashboard:**
- Payments → видим тестовый платёж
- Customers → видим тестового клиента

### Checklist урока
- [ ] Продукт создан в Stripe Dashboard
- [ ] Price ID добавлен в .env.local
- [ ] API route создан и работает
- [ ] Кнопка покупки работает
- [ ] Редирект на Stripe Checkout работает
- [ ] Тестовый платёж прошёл успешно
- [ ] Страница /success отображается

---

## Урок 10.9: Webhooks и статус подписки (30 мин)

### Цели урока
- Понять что такое Webhooks
- Настроить Webhook endpoint
- Обрабатывать события от Stripe

### Содержание

#### 1. Что такое Webhooks (5 мин)

**Проблема:**
```
Пользователь заплатил на Stripe...
Но ваш сервер об этом не знает!

Пользователь: "Я заплатил, где мой Premium?"
Сервер: "Не знаю ни о каких платежах..."
```

**Решение — Webhooks:**
```
Stripe сама "звонит" вам когда что-то происходит

Stripe ──── "Эй! Кто-то заплатил!" ────> Ваш сервер
                   (webhook)
Сервер: "Понял, включаю Premium!"
```

**Аналогия:**
```
Без webhooks: Вы постоянно звоните в банк
              "Мне заплатили? А сейчас? А сейчас?"

С webhooks:   Банк сам звонит вам
              "Вам пришёл перевод на $9.99"
```

**Важные события Stripe:**
| Событие | Когда происходит |
|---------|------------------|
| `checkout.session.completed` | Платёж прошёл успешно |
| `customer.subscription.created` | Подписка создана |
| `customer.subscription.deleted` | Подписка отменена |
| `invoice.payment_failed` | Платёж не прошёл |
| `invoice.paid` | Счёт оплачен |

#### 2. Создание Webhook endpoint (10 мин)

**Промпт для Cursor:**
```
Создай API route /api/stripe/webhook для обработки Stripe webhooks:

1. Файл: app/api/stripe/webhook/route.ts
2. Метод: POST
3. Функционал:
   - Получает raw body запроса
   - Проверяет подпись Stripe (stripe-signature header)
   - Использует STRIPE_WEBHOOK_SECRET
   - Обрабатывает события:
     * checkout.session.completed: логирует "Платёж прошёл!"
     * customer.subscription.deleted: логирует "Подписка отменена"
   - Возвращает 200 OK

4. Важно: использовать stripe.webhooks.constructEvent для верификации
```

**Ожидаемый код:**
```typescript
// app/api/stripe/webhook/route.ts
import { NextResponse } from 'next/server'
import { headers } from 'next/headers'
import { stripe } from '@/lib/stripe'
import Stripe from 'stripe'

export async function POST(request: Request) {
  const body = await request.text()
  const signature = headers().get('stripe-signature')!

  let event: Stripe.Event

  try {
    event = stripe.webhooks.constructEvent(
      body,
      signature,
      process.env.STRIPE_WEBHOOK_SECRET!
    )
  } catch (err: any) {
    console.error('Webhook signature verification failed:', err.message)
    return NextResponse.json(
      { error: 'Webhook signature verification failed' },
      { status: 400 }
    )
  }

  // Обработка событий
  switch (event.type) {
    case 'checkout.session.completed':
      const session = event.data.object as Stripe.Checkout.Session
      console.log('✅ Платёж прошёл!')
      console.log('Customer email:', session.customer_email)
      console.log('Customer ID:', session.customer)
      // Здесь можно обновить базу данных
      break

    case 'customer.subscription.deleted':
      const subscription = event.data.object as Stripe.Subscription
      console.log('❌ Подписка отменена')
      console.log('Subscription ID:', subscription.id)
      // Здесь можно отключить Premium
      break

    default:
      console.log(`Unhandled event type: ${event.type}`)
  }

  return NextResponse.json({ received: true })
}
```

#### 3. Настройка Stripe CLI для локального тестирования (10 мин)

**Проблема:**
```
Webhook endpoint: http://localhost:3000/api/stripe/webhook
Но Stripe не может достучаться до localhost!
```

**Решение: Stripe CLI**

**Установка:**
```bash
# macOS
brew install stripe/stripe-cli/stripe

# Windows (через scoop)
scoop install stripe

# Или скачать с: https://stripe.com/docs/stripe-cli
```

**Настройка:**
```bash
# Авторизация
stripe login

# Откроется браузер — разрешите доступ
```

**Запуск прослушивания webhooks:**
```bash
stripe listen --forward-to localhost:3000/api/stripe/webhook
```

**Что увидите:**
```
Ready! Your webhook signing secret is whsec_1234567890abcdef...
```

**Скопируйте `whsec_...` и добавьте в .env.local:**
```env
STRIPE_WEBHOOK_SECRET=whsec_ваш_секрет
```

**Важно:** Stripe CLI должен работать пока вы тестируете webhooks!

#### 4. Тестирование Webhook (5 мин)

**Вариант 1: Через реальный платёж**
1. Stripe CLI работает (`stripe listen ...`)
2. Делаем тестовый платёж на сайте
3. Смотрим консоль сервера — должно быть "✅ Платёж прошёл!"
4. В терминале Stripe CLI видим события

**Вариант 2: Триггер события вручную**
```bash
# В новом терминале (пока stripe listen работает)
stripe trigger checkout.session.completed
```

**Проверка в Stripe Dashboard:**
- Developers → Webhooks → Events
- Видим отправленные события и их статус

### Полный флоу тестирования

```
Терминал 1: npm run dev
Терминал 2: stripe listen --forward-to localhost:3000/api/stripe/webhook

Браузер:
1. Открываем страницу с кнопкой покупки
2. Нажимаем "Оформить Premium"
3. Вводим тестовую карту 4242...
4. Платим

Терминал 1 (сервер):
✅ Платёж прошёл!
Customer email: test@example.com

Терминал 2 (Stripe CLI):
2024-01-15 12:00:00   --> checkout.session.completed [evt_xxx]
2024-01-15 12:00:00  <--  [200] POST http://localhost:3000/api/stripe/webhook
```

### Checklist урока
- [ ] Webhook endpoint создан
- [ ] Stripe CLI установлен и настроен
- [ ] STRIPE_WEBHOOK_SECRET добавлен в .env.local
- [ ] События логируются в консоли
- [ ] Подпись проверяется корректно
- [ ] Понимаю как это работает в production

---

# Финальный проект: Data Aggregator

## Описание проекта

Веб-приложение, которое агрегирует данные из внешних API и предлагает Premium подписку для расширенных функций.

## Функционал

### Free версия (бесплатно):
- Погода для 1 города
- Курсы 3 валют (USD, EUR, RUB)
- Обновление раз в час

### Premium версия ($9.99/мес):
- Погода для любого города
- Все доступные валюты
- Обновление в реальном времени
- Без рекламы
- История данных

## Требования к проекту

### Git и GitHub (20 баллов)
- [ ] Проект в Git репозитории
- [ ] Минимум 10 commits с понятными сообщениями
- [ ] Использование веток для новых фич
- [ ] Код на GitHub (публичный репозиторий)
- [ ] README.md с инструкциями

### Внешние API (30 баллов)
- [ ] Интеграция с OpenWeatherMap API
- [ ] Интеграция с ExchangeRate-API
- [ ] Loading states для всех запросов
- [ ] Обработка ошибок с понятными сообщениями
- [ ] API ключи в .env.local (не в коде!)

### Stripe интеграция (30 баллов)
- [ ] Кнопка "Купить Premium" на странице
- [ ] Работающий Checkout Session
- [ ] Страницы /success и /cancel
- [ ] Webhook для обработки платежей
- [ ] Тестовый платёж проходит успешно

### UI/UX (20 баллов)
- [ ] Адаптивный дизайн (мобильный + десктоп)
- [ ] Приятный внешний вид
- [ ] Понятная навигация
- [ ] Индикация загрузки
- [ ] Сообщения об ошибках

## Структура проекта

```
data-aggregator/
├── app/
│   ├── api/
│   │   ├── weather/
│   │   │   └── route.ts
│   │   ├── exchange/
│   │   │   └── route.ts
│   │   └── stripe/
│   │       ├── create-checkout-session/
│   │       │   └── route.ts
│   │       └── webhook/
│   │           └── route.ts
│   ├── page.tsx              # Главная страница
│   ├── pricing/
│   │   └── page.tsx          # Страница с тарифами
│   ├── success/
│   │   └── page.tsx          # Успешная оплата
│   └── layout.tsx
├── components/
│   ├── WeatherWidget.tsx
│   ├── ExchangeWidget.tsx
│   ├── PurchaseButton.tsx
│   └── Header.tsx
├── lib/
│   ├── stripe.ts
│   └── stripe-client.ts
├── .env.local                # API ключи (НЕ на GitHub!)
├── .gitignore
├── README.md
└── package.json
```

## Промпт для создания проекта

```
Создай приложение Data Aggregator с Next.js:

1. Главная страница (app/page.tsx):
   - Header с навигацией
   - WeatherWidget (показывает погоду)
   - ExchangeWidget (показывает курсы валют)
   - Баннер "Попробуйте Premium" с кнопкой

2. Страница /pricing:
   - Сравнение Free и Premium
   - Кнопка PurchaseButton для Premium

3. Компоненты:
   - WeatherWidget: запрос к /api/weather, отображение данных
   - ExchangeWidget: запрос к /api/exchange, таблица курсов
   - PurchaseButton: создание Stripe checkout session

4. API Routes:
   - /api/weather?city=Moscow
   - /api/exchange?base=USD
   - /api/stripe/create-checkout-session
   - /api/stripe/webhook

Стили: Tailwind CSS, тёмная тема, современный дизайн.
```

## Критерии оценки

| Критерий | Баллы |
|----------|-------|
| Git репозиторий с историей commits | 10 |
| Код на GitHub с README | 10 |
| Погодный API работает | 15 |
| Курсы валют API работает | 15 |
| Loading и error states | 10 |
| Stripe Checkout работает | 15 |
| Webhook обрабатывает события | 10 |
| UI красивый и адаптивный | 10 |
| Код чистый и понятный | 5 |

**Всего: 100 баллов**

**Бонус:** +10 баллов за интеграцию третьего API (новости, прогноз на неделю, крипто-курсы)

---

## Тестовые данные для Stripe

### Успешные карты
```
Visa:        4242 4242 4242 4242
Mastercard:  5555 5555 5555 4444
Amex:        3782 822463 10005
```

### Карты с ошибками (для тестирования)
```
Отклонено:           4000 0000 0000 0002
Недостаточно средств: 4000 0000 0000 9995
Просрочена:          4000 0000 0000 0069
Неверный CVV:        4000 0000 0000 0127
```

### Для всех тестовых карт:
- Срок действия: любая дата в будущем
- CVV: любые 3 цифры (4 для Amex)
- Имя и адрес: любые

---

## Полезные ресурсы

### Git и GitHub
- [Git Book на русском](https://git-scm.com/book/ru/v2)
- [GitHub Docs](https://docs.github.com/ru)
- [GitHub Desktop](https://desktop.github.com/) — GUI для Git

### API
- [OpenWeatherMap API](https://openweathermap.org/api)
- [ExchangeRate-API](https://www.exchangerate-api.com/docs)
- [Public APIs List](https://github.com/public-apis/public-apis)

### Stripe
- [Stripe Docs](https://stripe.com/docs)
- [Stripe CLI](https://stripe.com/docs/stripe-cli)
- [Next.js + Stripe Guide](https://stripe.com/docs/checkout/quickstart?lang=next)

---

## Что дальше?

После этого модуля вы:
- Умеете работать с Git и GitHub
- Можете интегрировать любые внешние API
- Знаете как принимать платежи
- Готовы к реальным проектам!

**Следующий модуль (11 - Integration):**
- Объединение всех компонентов
- Production деплой на Vercel
- Настройка домена
- Финальный review

**Добро пожаловать в мир профессиональной разработки!**
