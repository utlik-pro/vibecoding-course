# Модуль 8: Базы данных для вайбкодера

## Обзор модуля
- **Длительность:** ~3.5 часа
- **Уровень:** Начинающий (zero code background)
- **Практический проект:** Task Manager Pro (менеджер задач с категориями)
- **Технологии:** Supabase, SQL, Next.js
- **Формат:** 90% практики, 10% теории

## Цели обучения
После завершения модуля студенты смогут:
1. Писать базовые SQL запросы (SELECT, INSERT, UPDATE, DELETE)
2. Понимать связи между таблицами (один-ко-многим)
3. Использовать JOIN для объединения данных
4. Фильтровать и сортировать данные
5. Группировать данные и считать статистику
6. Проектировать структуру базы данных для своих проектов

---

## Урок 8.1: SQL — язык общения с базой данных (20 мин)

### Цели урока
- Понять что такое SQL
- Научиться использовать SQL Editor в Supabase
- Выполнить первый запрос

### Теория (2 мин)
**SQL = язык вопросов к базе данных**
- Ты спрашиваешь — база отвечает
- Один язык для всех баз данных (почти)
- Похоже на английский: SELECT... FROM... WHERE...

### Практика (18 мин)

#### 1. Открываем SQL Editor
- Supabase → SQL Editor (иконка слева)
- Это как "консоль" для базы данных

#### 2. Создаём таблицу через SQL
Копируем и выполняем:
```sql
CREATE TABLE tasks (
  id SERIAL PRIMARY KEY,
  title TEXT NOT NULL,
  description TEXT,
  completed BOOLEAN DEFAULT false,
  priority TEXT DEFAULT 'medium',
  created_at TIMESTAMP DEFAULT NOW()
);
```

**Разбор:**
- `CREATE TABLE` — создать таблицу
- `SERIAL PRIMARY KEY` — автоматический уникальный номер
- `NOT NULL` — обязательное поле
- `DEFAULT` — значение по умолчанию

#### 3. Добавляем тестовые данные
```sql
INSERT INTO tasks (title, description, priority) VALUES
  ('Купить продукты', 'Молоко, хлеб, яйца', 'high'),
  ('Позвонить маме', NULL, 'high'),
  ('Сделать домашку', 'Математика и физика', 'medium'),
  ('Посмотреть курс', 'Вайбкодинг модуль 8', 'medium'),
  ('Погулять с собакой', NULL, 'low'),
  ('Оплатить интернет', 'До 15 числа', 'high'),
  ('Прочитать книгу', '30 минут', 'low');
```

#### 4. Первый SELECT
```sql
SELECT * FROM tasks;
```
- `SELECT` — выбрать
- `*` — все колонки
- `FROM tasks` — из таблицы tasks

Видим все наши задачи!

### Задание
Добавь ещё 3 своих задачи через INSERT.

---

## Урок 8.2: Фильтрация данных — WHERE (20 мин)

### Цели урока
- Научиться фильтровать данные
- Использовать операторы сравнения
- Комбинировать условия

### Практика

#### 1. Простая фильтрация
```sql
-- Только важные задачи
SELECT * FROM tasks WHERE priority = 'high';

-- Только невыполненные
SELECT * FROM tasks WHERE completed = false;
```

#### 2. Операторы сравнения
```sql
-- Не равно
SELECT * FROM tasks WHERE priority != 'low';

-- Задачи созданные сегодня
SELECT * FROM tasks WHERE created_at >= CURRENT_DATE;
```

#### 3. Комбинируем условия
```sql
-- Важные И невыполненные
SELECT * FROM tasks
WHERE priority = 'high' AND completed = false;

-- Важные ИЛИ средние
SELECT * FROM tasks
WHERE priority = 'high' OR priority = 'medium';

-- То же самое через IN
SELECT * FROM tasks
WHERE priority IN ('high', 'medium');
```

#### 4. Поиск по тексту
```sql
-- Задачи со словом "книг" в названии
SELECT * FROM tasks WHERE title LIKE '%книг%';

-- Задачи начинающиеся на "По"
SELECT * FROM tasks WHERE title LIKE 'По%';
```

**Шпаргалка:**
- `%` — любые символы (ноль или больше)
- `_` — ровно один символ

#### 5. NULL — особый случай
```sql
-- Задачи БЕЗ описания
SELECT * FROM tasks WHERE description IS NULL;

-- Задачи С описанием
SELECT * FROM tasks WHERE description IS NOT NULL;
```

### Мини-задания (делаем вместе)
1. Найди все задачи с низким приоритетом
2. Найди невыполненные задачи с описанием
3. Найди задачи где в названии есть буква "а"

---

## Урок 8.3: Сортировка и лимиты (15 мин)

### Цели урока
- Сортировать результаты
- Ограничивать количество записей
- Выбирать конкретные колонки

### Практика

#### 1. Сортировка — ORDER BY
```sql
-- По дате создания (новые первые)
SELECT * FROM tasks ORDER BY created_at DESC;

-- По приоритету (алфавит)
SELECT * FROM tasks ORDER BY priority ASC;

-- Сначала невыполненные, потом по приоритету
SELECT * FROM tasks
ORDER BY completed ASC, priority DESC;
```

**Запомни:**
- `ASC` — по возрастанию (A→Z, 1→9) — по умолчанию
- `DESC` — по убыванию (Z→A, 9→1)

#### 2. Ограничение количества — LIMIT
```sql
-- Только первые 3 задачи
SELECT * FROM tasks LIMIT 3;

-- Топ-5 важных задач
SELECT * FROM tasks
WHERE priority = 'high'
ORDER BY created_at DESC
LIMIT 5;
```

#### 3. Пропустить записи — OFFSET
```sql
-- Пропустить первые 2, взять следующие 3 (пагинация)
SELECT * FROM tasks LIMIT 3 OFFSET 2;
```

#### 4. Выбор конкретных колонок
```sql
-- Только название и приоритет
SELECT title, priority FROM tasks;

-- Переименование колонки в результате
SELECT title AS "Задача", priority AS "Важность" FROM tasks;
```

### Мини-задания
1. Покажи 3 самые новые задачи
2. Покажи только названия невыполненных задач

---

## Урок 8.4: Изменение и удаление данных (20 мин)

### Цели урока
- Обновлять данные через UPDATE
- Удалять данные через DELETE
- Понимать важность WHERE в этих операциях

### Практика

#### 1. UPDATE — изменение данных
```sql
-- Отметить задачу как выполненную
UPDATE tasks
SET completed = true
WHERE id = 1;

-- Изменить приоритет
UPDATE tasks
SET priority = 'high'
WHERE title = 'Позвонить маме';

-- Изменить несколько полей
UPDATE tasks
SET completed = true, priority = 'low'
WHERE id = 3;
```

#### 2. Массовое обновление
```sql
-- Все задачи с низким приоритетом сделать средними
UPDATE tasks
SET priority = 'medium'
WHERE priority = 'low';
```

#### 3. DELETE — удаление данных
```sql
-- Удалить одну задачу
DELETE FROM tasks WHERE id = 5;

-- Удалить все выполненные
DELETE FROM tasks WHERE completed = true;
```

#### 4. ВАЖНО: Всегда используй WHERE!
```sql
-- ОПАСНО! Удалит ВСЕ задачи!
DELETE FROM tasks;

-- ОПАСНО! Обновит ВСЕ задачи!
UPDATE tasks SET completed = true;
```

**Правило безопасности:**
Перед UPDATE/DELETE сначала сделай SELECT с тем же WHERE!
```sql
-- 1. Проверяем что выберется
SELECT * FROM tasks WHERE priority = 'low';

-- 2. Только потом удаляем
DELETE FROM tasks WHERE priority = 'low';
```

#### 5. Восстановим данные
```sql
-- Очистим и заново наполним
DELETE FROM tasks;

INSERT INTO tasks (title, description, priority, completed) VALUES
  ('Купить продукты', 'Молоко, хлеб, яйца', 'high', false),
  ('Позвонить маме', NULL, 'high', false),
  ('Сделать домашку', 'Математика и физика', 'medium', true),
  ('Посмотреть курс', 'Вайбкодинг модуль 8', 'medium', false),
  ('Погулять с собакой', NULL, 'low', false),
  ('Оплатить интернет', 'До 15 числа', 'high', false),
  ('Прочитать книгу', '30 минут', 'low', true);
```

---

## Урок 8.5: Связи между таблицами (30 мин)

### Цели урока
- Понять зачем нужны связи
- Создать связанные таблицы
- Понять концепцию внешнего ключа

### Теория (5 мин)
**Проблема:** Хотим добавить категории к задачам.

**Плохой способ — хранить текстом:**
```
Задача: "Купить молоко", Категория: "Покупки"
Задача: "Купить хлеб", Категория: "Покупки"
Задача: "Купить яйца", Категория: "покупки"  -- опечатка!
```

**Хороший способ — отдельная таблица:**
```
Таблица categories: id=1, name="Покупки"
Таблица tasks: category_id=1 (ссылка на categories)
```

### Практика

#### 1. Создаём таблицу категорий
```sql
CREATE TABLE categories (
  id SERIAL PRIMARY KEY,
  name TEXT NOT NULL UNIQUE,
  color TEXT DEFAULT '#3B82F6',
  icon TEXT DEFAULT '📁'
);
```

#### 2. Добавляем категории
```sql
INSERT INTO categories (name, color, icon) VALUES
  ('Работа', '#EF4444', '💼'),
  ('Дом', '#10B981', '🏠'),
  ('Учёба', '#8B5CF6', '📚'),
  ('Здоровье', '#F59E0B', '💪'),
  ('Покупки', '#EC4899', '🛒'),
  ('Личное', '#6366F1', '👤');
```

#### 3. Добавляем связь в tasks
```sql
-- Добавляем колонку для связи
ALTER TABLE tasks ADD COLUMN category_id INTEGER;

-- Создаём внешний ключ (foreign key)
ALTER TABLE tasks
ADD CONSTRAINT fk_category
FOREIGN KEY (category_id) REFERENCES categories(id);
```

**Что такое внешний ключ?**
- Это "ссылка" на запись в другой таблице
- База данных проверяет что такая категория существует
- Нельзя указать несуществующую category_id

#### 4. Привязываем задачи к категориям
```sql
-- Посмотрим id категорий
SELECT * FROM categories;

-- Обновляем задачи
UPDATE tasks SET category_id = 5 WHERE title LIKE '%Купить%';        -- Покупки
UPDATE tasks SET category_id = 4 WHERE title LIKE '%собакой%';       -- Здоровье
UPDATE tasks SET category_id = 1 WHERE title LIKE '%интернет%';      -- Работа
UPDATE tasks SET category_id = 3 WHERE title LIKE '%курс%';          -- Учёба
UPDATE tasks SET category_id = 3 WHERE title LIKE '%домашку%';       -- Учёба
UPDATE tasks SET category_id = 6 WHERE title LIKE '%маме%';          -- Личное
UPDATE tasks SET category_id = 6 WHERE title LIKE '%книгу%';         -- Личное
```

#### 5. Проверяем связь
```sql
SELECT * FROM tasks;
```

Теперь у каждой задачи есть category_id!

---

## Урок 8.6: JOIN — объединение таблиц (30 мин)

### Цели урока
- Понять разные типы JOIN
- Получать данные из связанных таблиц
- Использовать JOIN на практике

### Теория (3 мин)
**JOIN = соединение таблиц**

Представь две таблицы как два списка:
- Список задач (с category_id)
- Список категорий (с id и названием)

JOIN "склеивает" их по совпадающим значениям.

### Практика

#### 1. INNER JOIN — только совпадения
```sql
SELECT
  tasks.title,
  tasks.priority,
  categories.name AS category_name,
  categories.icon
FROM tasks
INNER JOIN categories ON tasks.category_id = categories.id;
```

**Что происходит:**
- Берём задачу
- Ищем категорию с таким же id как category_id задачи
- Показываем вместе

#### 2. Сокращённая запись с алиасами
```sql
SELECT
  t.title,
  t.priority,
  c.name AS category,
  c.icon
FROM tasks t
INNER JOIN categories c ON t.category_id = c.id;
```

`t` и `c` — короткие имена (алиасы) для таблиц.

#### 3. LEFT JOIN — все задачи + категории если есть
```sql
SELECT
  t.title,
  t.priority,
  c.name AS category
FROM tasks t
LEFT JOIN categories c ON t.category_id = c.id;
```

**Разница:**
- `INNER JOIN` — только задачи У КОТОРЫХ ЕСТЬ категория
- `LEFT JOIN` — ВСЕ задачи, даже без категории (будет NULL)

#### 4. Фильтрация с JOIN
```sql
-- Все задачи из категории "Учёба"
SELECT t.title, t.completed
FROM tasks t
INNER JOIN categories c ON t.category_id = c.id
WHERE c.name = 'Учёба';

-- Невыполненные задачи категории "Работа"
SELECT t.title, c.icon
FROM tasks t
INNER JOIN categories c ON t.category_id = c.id
WHERE c.name = 'Работа' AND t.completed = false;
```

#### 5. Сортировка с JOIN
```sql
-- Задачи отсортированные по категории
SELECT t.title, c.name AS category, t.priority
FROM tasks t
INNER JOIN categories c ON t.category_id = c.id
ORDER BY c.name, t.priority DESC;
```

### Мини-задания
1. Покажи все задачи с иконкой их категории
2. Найди все задачи из категории "Личное"
3. Покажи невыполненные задачи с названием категории

---

## Урок 8.7: Агрегация и группировка (25 мин)

### Цели урока
- Считать количество записей (COUNT)
- Использовать другие агрегатные функции
- Группировать данные (GROUP BY)

### Практика

#### 1. COUNT — считаем записи
```sql
-- Сколько всего задач?
SELECT COUNT(*) FROM tasks;

-- Сколько невыполненных?
SELECT COUNT(*) FROM tasks WHERE completed = false;

-- Сколько задач с описанием?
SELECT COUNT(description) FROM tasks;
```

**Важно:** `COUNT(*)` считает все строки, `COUNT(column)` — только не NULL.

#### 2. Другие агрегатные функции
```sql
-- Для примера добавим числовое поле (время в минутах)
ALTER TABLE tasks ADD COLUMN estimated_minutes INTEGER DEFAULT 30;

UPDATE tasks SET estimated_minutes = 15 WHERE priority = 'low';
UPDATE tasks SET estimated_minutes = 30 WHERE priority = 'medium';
UPDATE tasks SET estimated_minutes = 60 WHERE priority = 'high';

-- Сумма всех минут
SELECT SUM(estimated_minutes) AS total_minutes FROM tasks;

-- Среднее время
SELECT AVG(estimated_minutes) AS avg_minutes FROM tasks;

-- Минимум и максимум
SELECT
  MIN(estimated_minutes) AS min_time,
  MAX(estimated_minutes) AS max_time
FROM tasks;
```

#### 3. GROUP BY — группировка
```sql
-- Сколько задач каждого приоритета?
SELECT priority, COUNT(*) AS count
FROM tasks
GROUP BY priority;

-- Сколько выполненных и невыполненных?
SELECT completed, COUNT(*) AS count
FROM tasks
GROUP BY completed;
```

#### 4. GROUP BY с JOIN
```sql
-- Сколько задач в каждой категории?
SELECT c.name, c.icon, COUNT(t.id) AS task_count
FROM categories c
LEFT JOIN tasks t ON c.id = t.category_id
GROUP BY c.id, c.name, c.icon
ORDER BY task_count DESC;
```

#### 5. HAVING — фильтрация групп
```sql
-- Категории с более чем 1 задачей
SELECT c.name, COUNT(t.id) AS task_count
FROM categories c
LEFT JOIN tasks t ON c.id = t.category_id
GROUP BY c.id, c.name
HAVING COUNT(t.id) > 1;
```

**Разница WHERE и HAVING:**
- `WHERE` — фильтрует строки ДО группировки
- `HAVING` — фильтрует группы ПОСЛЕ группировки

#### 6. Полезная статистика
```sql
-- Статистика по категориям
SELECT
  c.name AS category,
  c.icon,
  COUNT(t.id) AS total,
  COUNT(CASE WHEN t.completed THEN 1 END) AS done,
  COUNT(CASE WHEN NOT t.completed THEN 1 END) AS pending
FROM categories c
LEFT JOIN tasks t ON c.id = t.category_id
GROUP BY c.id, c.name, c.icon
ORDER BY total DESC;
```

---

## Урок 8.8: Интеграция SQL в приложение (35 мин)

### Цели урока
- Использовать SQL запросы в API routes
- Создать API для Task Manager
- Реализовать фильтрацию и статистику

### Практика

#### 1. Подготовка проекта
Промпт для Cursor:
```
Создай новый Next.js проект task-manager-pro с TypeScript и Tailwind.
Настрой подключение к Supabase как в предыдущем модуле.
```

#### 2. API для получения задач с категориями
Промпт для Cursor:
```
Создай API route /api/tasks который:
1. Использует Supabase JS client
2. Получает все задачи с информацией о категории (используя Supabase relations)
3. Поддерживает query параметры:
   - category: фильтр по category_id
   - completed: true/false
   - priority: high/medium/low
4. Сортирует по created_at DESC
5. Возвращает задачи с полем category: { name, color, icon }
```

#### 3. API для статистики
Промпт для Cursor:
```
Создай API route /api/stats который возвращает:
1. Общее количество задач
2. Количество выполненных
3. Количество по каждому приоритету
4. Количество по каждой категории с названием и цветом
```

#### 4. API для категорий
Промпт для Cursor:
```
Создай API route /api/categories который:
1. GET — возвращает все категории с количеством задач в каждой
2. POST — создаёт новую категорию
```

#### 5. Supabase Relations (альтернатива JOIN в коде)
```typescript
// Вместо ручного JOIN, Supabase умеет делать так:
const { data } = await supabase
  .from('tasks')
  .select(`
    id,
    title,
    completed,
    priority,
    category:categories (
      id,
      name,
      color,
      icon
    )
  `)
  .order('created_at', { ascending: false });
```

#### 6. Создание UI
Промпт для Cursor:
```
Создай главную страницу Task Manager Pro:
1. Сверху — статистика (всего задач, выполнено, осталось)
2. Слева — список категорий с количеством задач (фильтр)
3. Справа — список задач с:
   - Чекбокс для отметки выполнения
   - Название и описание
   - Иконка и цвет категории
   - Приоритет (цветная метка)
4. Форма добавления задачи:
   - Название
   - Описание
   - Выбор категории (dropdown)
   - Выбор приоритета
5. Кнопка удаления для каждой задачи
6. Фильтрация по категории при клике на категорию слева
```

### Результат
Task Manager с фильтрацией по категориям и статистикой!

---

## Урок 8.9: Продвинутые техники (25 мин)

### Цели урока
- Узнать про индексы
- Понять транзакции
- Изучить полезные SQL функции

### Содержание

#### 1. Индексы — ускорение поиска
```sql
-- Создаём индекс для быстрого поиска по category_id
CREATE INDEX idx_tasks_category ON tasks(category_id);

-- Индекс для поиска невыполненных задач
CREATE INDEX idx_tasks_completed ON tasks(completed) WHERE completed = false;
```

**Когда нужны индексы:**
- Колонка часто используется в WHERE
- Колонка используется в JOIN
- Таблица большая (тысячи+ записей)

#### 2. Полезные SQL функции
```sql
-- Текущая дата и время
SELECT NOW(), CURRENT_DATE, CURRENT_TIME;

-- Форматирование даты
SELECT title, TO_CHAR(created_at, 'DD.MM.YYYY') AS date
FROM tasks;

-- Работа с текстом
SELECT
  UPPER(title) AS upper_title,
  LOWER(title) AS lower_title,
  LENGTH(title) AS title_length
FROM tasks;

-- COALESCE — замена NULL
SELECT
  title,
  COALESCE(description, 'Нет описания') AS description
FROM tasks;
```

#### 3. Подзапросы
```sql
-- Задачи из самой популярной категории
SELECT * FROM tasks
WHERE category_id = (
  SELECT category_id
  FROM tasks
  GROUP BY category_id
  ORDER BY COUNT(*) DESC
  LIMIT 1
);

-- Категории без задач
SELECT * FROM categories
WHERE id NOT IN (SELECT DISTINCT category_id FROM tasks WHERE category_id IS NOT NULL);
```

#### 4. Транзакции (для сложных операций)
```sql
-- Переносим все задачи из одной категории в другую
BEGIN;
  UPDATE tasks SET category_id = 2 WHERE category_id = 1;
  DELETE FROM categories WHERE id = 1;
COMMIT;

-- Если что-то пошло не так
-- ROLLBACK;
```

#### 5. Views — сохранённые запросы
```sql
-- Создаём "виртуальную таблицу" с задачами и категориями
CREATE VIEW tasks_with_categories AS
SELECT
  t.id,
  t.title,
  t.completed,
  t.priority,
  c.name AS category_name,
  c.icon AS category_icon,
  c.color AS category_color
FROM tasks t
LEFT JOIN categories c ON t.category_id = c.id;

-- Используем как обычную таблицу
SELECT * FROM tasks_with_categories WHERE priority = 'high';
```

---

## Финальный проект: Task Manager Pro

### Описание
Полноценный менеджер задач с категориями, фильтрацией и статистикой.

### Требования

#### База данных
- [ ] Таблица tasks с полями: id, title, description, completed, priority, category_id, created_at
- [ ] Таблица categories с полями: id, name, color, icon
- [ ] Внешний ключ между tasks и categories
- [ ] Минимум 5 категорий
- [ ] Минимум 10 задач

#### API Endpoints
- [ ] GET /api/tasks — список задач с категориями
- [ ] POST /api/tasks — создание задачи
- [ ] PATCH /api/tasks/[id] — обновление задачи
- [ ] DELETE /api/tasks/[id] — удаление задачи
- [ ] GET /api/categories — список категорий с количеством задач
- [ ] GET /api/stats — статистика

#### Фронтенд
- [ ] Отображение статистики (всего / выполнено / осталось)
- [ ] Список категорий с фильтрацией
- [ ] Список задач с чекбоксами
- [ ] Форма добавления задачи
- [ ] Визуальное отображение приоритета
- [ ] Иконки и цвета категорий

### Критерии оценки (100 баллов)
| Критерий | Баллы |
|----------|-------|
| Таблицы созданы правильно | 10 |
| Связь между таблицами работает | 10 |
| GET /api/tasks с категориями | 15 |
| POST/PATCH/DELETE работают | 15 |
| Статистика отображается | 10 |
| Фильтрация по категориям | 15 |
| Форма добавления работает | 15 |
| UI выглядит хорошо | 10 |

**Бонус:** +10 баллов за поиск по названию задач

---

## SQL Шпаргалка

### Основные команды
```sql
-- Выборка
SELECT * FROM table;
SELECT col1, col2 FROM table;
SELECT * FROM table WHERE condition;
SELECT * FROM table ORDER BY col DESC;
SELECT * FROM table LIMIT 10;

-- Вставка
INSERT INTO table (col1, col2) VALUES (val1, val2);

-- Обновление
UPDATE table SET col1 = val1 WHERE condition;

-- Удаление
DELETE FROM table WHERE condition;
```

### JOIN
```sql
-- Внутреннее соединение
SELECT * FROM t1 INNER JOIN t2 ON t1.id = t2.t1_id;

-- Левое соединение (все из левой + совпадения из правой)
SELECT * FROM t1 LEFT JOIN t2 ON t1.id = t2.t1_id;
```

### Агрегация
```sql
-- Подсчёт
SELECT COUNT(*) FROM table;
SELECT col, COUNT(*) FROM table GROUP BY col;

-- Другие функции
SELECT SUM(col), AVG(col), MIN(col), MAX(col) FROM table;
```

### Операторы WHERE
```sql
=, !=, <, >, <=, >=
AND, OR, NOT
IN (val1, val2)
BETWEEN val1 AND val2
LIKE '%pattern%'
IS NULL, IS NOT NULL
```

---

## Дополнительные ресурсы

### Документация
- [Supabase SQL](https://supabase.com/docs/guides/database)
- [PostgreSQL Tutorial](https://www.postgresqltutorial.com/)

### Практика SQL
- [SQLBolt](https://sqlbolt.com/) — интерактивные уроки
- [SQL Murder Mystery](https://mystery.knightlab.com/) — детективная игра на SQL

### Инструменты
- Supabase SQL Editor — встроенный в Supabase
- TablePlus — GUI для баз данных (бесплатная версия)

---

## Что дальше?

В следующих модулях:
- **Модуль 9:** Аутентификация (Row Level Security, защита данных)
- **Модуль 10:** Real-time данные (live updates)
- **Модуль 11:** Деплой и production

Теперь ты умеешь:
- Писать SQL запросы
- Связывать таблицы
- Делать статистику
- Строить API с базой данных
